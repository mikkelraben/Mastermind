<UserControl
    x:Class="Mastermind.Board"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mastermind"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Name="MainGrid" Padding="4" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">

        </Grid>
        <Button Name="CheckResult" Click="CheckResult_Click" HorizontalAlignment="Center" VerticalAlignment="Top">
            Result
            <Button.TranslationTransition>
                <Vector3Transition/>
            </Button.TranslationTransition>
        </Button>

        
        <StackPanel Name="Controls" Grid.Row="1" HorizontalAlignment="Center" Orientation="Vertical">
            <Grid x:Name="SecretRow" Visibility="Collapsed" Height="52"/>
            <Button Margin="4" Click="GiveUp_Click" HorizontalAlignment="Center">Give Up?</Button>
        </StackPanel>
    </Grid>


</UserControl>
